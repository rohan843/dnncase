/**
 * @deprecated This component is to be no longer used. Use the `useActivatableEntity` hook instead.
 * 
 * @throws An error if this component is used.
 */
function ActivatableEntity() {
  throw Error(
    "This component is not to be used any more. Use the `useActivatableEntity` hook instead."
  );
}

export default ActivatableEntity;
